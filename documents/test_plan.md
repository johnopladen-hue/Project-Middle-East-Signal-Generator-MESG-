# Test Plan — Project MESG

> All tests for the project, tracked in tabular format. Update as build orders/TDDs introduce new components requiring test coverage.

| Test ID | Component/Feature | Test Type | Description | Expected Result | Status | Date | Notes |
|---|---|---|---|---|---|---|---|
| T-001 | Keel gate (`keel_proof.py`) | Unit + gate proof | Self-contained pytest run in CI (`.github/workflows/ci.yml`), required as a status check on `main`. Not MESG feature code — proves the merge-on-green mechanism per Keel Principle 6. | A failing `keel_proof` test blocks PR merge (even for the repo owner); a passing one merges and the `deploy` job runs. | Passing | 2026-09-06 | Witnessed live on PR #4 (5-move proof); see `documents/findings.md` 2026-09-06 entry for the full result. Superseded as the required check by T-002/T-003 once the frontend suite existed (CI job renamed `test-backend`/`test-frontend`). |
| T-002 | Backend suite (`backend/tests/`) | Unit | Grading engine (corroboration cap, TDD §9.2), signal taxonomy, argon2 auth, whitelist-enforcing delivery, health endpoint — all self-contained (Keel Principle 5), no network. | All pass offline; corroboration-cap test proves the guard by tripping it (5 sources + DIRECT access still capped at 3 without an artefact). | Passing | 2026-09-06 | 20 tests, CI job `test-backend`. |
| T-003 | Frontend primitives (`frontend/src/components/primitives/*.test.jsx`) | Unit | Order 2 UI primitives: Badge (grade tone always carries text), Modal (dialog semantics + Escape-to-close), Tabs (arrow-key navigation + panel visibility), FormField (error linked via aria-describedby). All via Vitest + RTL, jsdom, no network (Keel Principle 5, UI-spec §9). | All pass offline in seconds. | Passing | 2026-09-06 | 8 tests, CI job `test-frontend`. Coverage of the remaining §9 requirements (provenance rendering, grade ceiling, role gating, release blast-radius) lands as each order builds the component it applies to. |
| T-004 | Frontend auth flow (`frontend/src/app/AuthFlow.test.jsx`) | Unit (MSW-mocked) | Order 3: unauthenticated redirect to `/login`, successful login redirecting to the originally-requested route, generic invalid-credentials message, rate-limit message with server-supplied retry delay. All against MSW fixtures, no real network (UI-spec §9). | All pass offline. | Passing | 2026-09-06 | 4 tests, CI job `test-frontend`. |
| T-005 | Backend auth endpoints (`backend/tests/test_auth_endpoints.py`) | Unit (isolated temp SQLite) | `/auth/login`, `/auth/logout`, `/auth/me`, and the in-memory rate limiter (5 attempts / 60s). Self-contained: a fresh temp SQLite file per test session, no shared state with any real dev database (Keel Principle 5). | Wrong password → 401; correct password → session cookie + 200; `/auth/me` roundtrips the session; logout clears it; the 6th failed attempt in the window → 429 with `Retry-After`. | Passing | 2026-09-06 | 5 tests, CI job `test-backend`. |
| T-006 | Frontend app shell + role gating (`frontend/src/app/AppShell.test.jsx`) | Unit (MSW-mocked) | Order 4: Admin nav link visible for `admin`, hidden for `viewer` (F-7); `PipelineStatusStrip` renders last-run + silent-source count from fixture data. | All pass offline. | Passing | 2026-09-06 | 3 tests, CI job `test-frontend`. |
| T-007 | Pipeline status endpoint (`backend/tests/test_pipeline_status.py`) | Unit (isolated temp SQLite) | `GET /pipeline/status` — not in TDD §7's table, added to back the PipelineStatusStrip domain component (UI-spec §6); silent-source count derived from `Source.last_seen_at` vs. a 6h threshold. | Zero sources → 0 silent; a source with no `last_seen_at` counts as silent. | Passing | 2026-09-06 | 2 tests, CI job `test-backend`. |
| T-008 | Provenance components (`frontend/src/domain/{normalizeFact,SourceTrail,GradeBlock,ProvenanceChip}.test.*`) | Unit | Order 5: the three explicit PROOF requirements — (1) an artefact-less fact renders as "Unverified", never a graded finding; (2) `GradeBlock` never displays above 3 without a corroborating artefact, even if the `grade` prop itself says 5; (3) a `ProvenanceChip` reaches the raw item's original + translation text in one click. | All pass offline. | Passing | 2026-09-06 | 9 tests, CI job `test-frontend`. |
| T-009 | Backend briefs endpoints (`backend/tests/test_briefs.py`) | Unit (isolated temp SQLite) | `GET /briefs?type=`, `GET /briefs/{id}`. `content_json` is served as-is — this router does not join Story/Analysis/RawItem, since no ingestion pipeline exists yet to populate them (documents/decisions.md open list). | Empty list when none exist; roundtrip of a hand-inserted Brief's `content_json`; missing id → 404. | Passing | 2026-09-06 | 3 tests, CI job `test-backend`. |
| T-010 | Dashboard / daily brief (`frontend/src/routes/Dashboard.test.jsx`) | Unit (MSW-mocked) | Order 6: all four data-view states (§5.1) — loading skeleton, empty ("no brief yet today"), error + retry, loaded — plus a `BriefItemCard` expanding headline → facts (with `SourceTrail`) → analysis → `GradeBlock`. | All pass offline. | Passing | 2026-09-06 | 4 tests, CI job `test-frontend`. |
| T-011 | Backend stories endpoint (`backend/tests/test_stories.py`) | Unit (isolated temp SQLite) | `GET /stories/{id}` — real joins across Story/Analysis/SourceAssessment/Divergence/RawItem (unlike Brief's JSON-blob approach). `has_corroborating_artefact` derived as `grade >= 4` (see `findings.md` 2026-09-06 — a known simplification, not reversible for grades 1-3). | Missing id → 404; full detail roundtrips correctly; a story with no analysis yet returns null grade and `has_corroborating_artefact: false`. | Passing | 2026-09-06 | 3 tests, CI job `test-backend`. |
| T-012 | Story detail (`frontend/src/routes/Story.test.jsx`) | Unit (MSW-mocked) | Order 7's three explicit PROOF requirements — (1) `DivergencePanel` shows both summaries plus divergence/convergence lists; (2) every fact reaches its raw item in one click; (3) `GradeBlock` shows contrary evidence. Also covers `SourceAssessmentTable` rendering. | All pass offline. | Passing | 2026-09-06 | 4 tests, CI job `test-frontend`. |
| T-013 | Backend signals + recipients endpoints (`backend/tests/test_signals_api.py`, `test_recipients.py`) | Unit (isolated temp SQLite) | `GET/POST /signals*` (filters, release, suppress — admin-only via `require_admin`), `GET /recipients/summary` (admin-only). Release currently only flips status; it does not yet dispatch real email/SMS (documents/findings.md — provider decisions still open). | Viewer → 403 on release/suppress; unauthenticated → 401; admin release/suppress updates status and is reflected on re-fetch; recipient counts split correctly by channel. | Passing | 2026-09-06 | 7 tests, CI job `test-backend`. Named `test_signals_api.py` (not `test_signals.py`) to avoid colliding with the pre-existing event-taxonomy test module. |
| T-014 | Alerts + review-and-release (`frontend/src/domain/ReviewReleaseControls.test.jsx`, `frontend/src/routes/Alerts.test.jsx`) | Unit (MSW-mocked) | Order 8's four explicit PROOF requirements — (1) a viewer sees no release/suppress controls at all; (2) the release confirm modal shows the correct recipient counts *before* any call is made; (3) no release call fires until confirmed; (4) suppress uses only a light inline Yes/No, not the full modal. Plus `Alerts` list empty/loaded states with `SeverityMark`/`ImminentPill`. | All pass offline. | Passing | 2026-09-06 | 5 tests, CI job `test-frontend`. |
| T-015 | Weekly (`frontend/src/routes/Weekly.test.jsx`) | Unit (MSW-mocked) | Order 9's three explicit PROOF requirements — latest weekly renders by default; a past weekly loads by id; empty/error states render. Reuses the existing `/briefs?type=` and `/briefs/{id}` endpoints (no backend change needed — `type=weekly` was already supported). | All pass offline. | Passing | 2026-09-06 | 4 tests, CI job `test-frontend`. |
| T-016 | Backend admin endpoints (`backend/tests/test_admin.py`) | Unit (isolated temp SQLite) | Admin-only (`require_admin`) CRUD for `/admin/sources`, `/admin/recipients`, and a `/admin/settings` singleton (free-form JSON — actual threshold/cadence fields still open, documents/decisions.md). | Viewer/unauthenticated → 403/401 on every admin route; admin can create/list/patch sources and recipients; settings roundtrip through PATCH then GET. | Passing | 2026-09-06 | 6 tests, CI job `test-backend`. |
| T-017 | Admin pages + role gating (`frontend/src/routes/admin/{Sources,Recipients}.test.jsx`, `frontend/src/app/ProtectedRoute.test.jsx`) | Unit (MSW-mocked) | Order 10's PROOF requirements — list/add works for Sources; the silence flag shows for a source with no `last_seen_at`; Recipients PII (email/phone) is masked by default and reveals on click; the Private-repo tripwire note (TDD §10) is present; a viewer hitting an admin-only route directly is redirected away (not just nav-hidden). | All pass offline. | Passing | 2026-09-06 | 6 tests, CI job `test-frontend`. |

**Status legend:** Not Started · In Progress · Passing · Failing · Blocked

**Test Type examples:** Unit · Integration · End-to-End · Manual · Security · Performance
